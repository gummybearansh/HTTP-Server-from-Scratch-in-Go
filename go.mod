module server

// replace github.com/gummybearansh/token-bucket-limiter => ../03-token-bucket-limiter

go 1.26.4

require github.com/gummybearansh/token-bucket-limiter v1.0.0

replace github.com/gummybearansh/token-bucket-limiter => ../03-Token-Bucket-Limiter
